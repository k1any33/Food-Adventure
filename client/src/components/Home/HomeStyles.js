import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  parentDiv: {
    position: 'fixed',
    bottom: '2px',
  },
  pagination: {
    borderRadius: 4,
    // margin: '0px auto',
    padding: '10px',
  },
}));
